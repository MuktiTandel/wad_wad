import 'package:flutter/material.dart';

class MutualScreen extends StatelessWidget {
  const MutualScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
